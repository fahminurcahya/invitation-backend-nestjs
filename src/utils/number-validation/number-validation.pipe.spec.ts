import { NumberValidationPipe } from './number-validation.pipe';

describe('NumberValidationPipe', () => {
  it('should be defined', () => {
    expect(new NumberValidationPipe()).toBeDefined();
  });
});
